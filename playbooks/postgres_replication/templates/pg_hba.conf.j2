local   all             all                                    peer
host    all             all             127.0.0.1/32           scram-sha-256
host    all             all             ::1/128                scram-sha-256

local   replication     all                                    peer
host    replication     all             127.0.0.1/32           scram-sha-256
host    replication     all             ::1/128                scram-sha-256
host    replication  replication    {{ master_ip }}            scram-sha-256
host    replication  replication    {{ slave_ip }}             scram-sha-256
host    postgres     postgres       {{ app_ip }}               scram-sha-256
host    postgres     postgres       {{ manager_ip }}           scram-sha-256
